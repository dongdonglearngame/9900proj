\documentclass[11pt,a4paper]{article}

% -----------------------------------------------------------------------------
% COMP9900 Project 18 final report (9900-F09C-ALMOND).
% Built from the client-review draft after client feedback on 1 August 2026.
% Remaining manual inserts before submission: the two screenshot figures.
% -----------------------------------------------------------------------------

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[margin=2.3cm]{geometry}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{array}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{listings}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{tikz}
\usepackage{amssymb}
\usepackage{amsmath}

\usetikzlibrary{arrows.meta,positioning,shapes.geometric}

\definecolor{UNSWGold}{HTML}{FFD500}
\definecolor{Navy}{HTML}{0B1F3A}
\definecolor{Blue}{HTML}{2056A8}
\definecolor{PaleBlue}{HTML}{EDF4FF}
\definecolor{PaleGold}{HTML}{FFF8D8}
\definecolor{PaleGrey}{HTML}{F4F6F8}
\definecolor{DarkGrey}{HTML}{445064}

\hypersetup{
  colorlinks=true,
  linkcolor=Blue,
  citecolor=Blue,
  urlcolor=Blue,
  pdftitle={Project 18 Final Report},
  pdfauthor={9900-F09C-ALMOND}
}

\pagestyle{fancy}
\fancyhf{}
\lhead{COMP9900 Project 18}
\rhead{9900-F09C-ALMOND}
\cfoot{\thepage}
\setlength{\headheight}{14pt}

\setlist[itemize]{leftmargin=1.6em,itemsep=0.25em,topsep=0.4em}
\setlist[enumerate]{leftmargin=1.8em,itemsep=0.25em,topsep=0.4em}
\renewcommand{\arraystretch}{1.22}
\setcounter{tocdepth}{2}

\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\lstdefinestyle{terminal}{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{PaleGrey},
  frame=single,
  rulecolor=\color{DarkGrey!35},
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  xleftmargin=0.4em,
  xrightmargin=0.4em
}

\lstdefinestyle{prompt}{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color{PaleGrey},
  frame=single,
  rulecolor=\color{DarkGrey!35},
  breaklines=true,
  breakindent=0pt,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  xleftmargin=0.4em,
  xrightmargin=0.4em
}

\newcommand{\stratone}{Strategy 1 (Propose and Verify)}
\newcommand{\strattwo}{Strategy 2 (Importance-Guided Infilling)}
\newcommand{\stratthree}{Strategy 3 (Concept-Guided Editing)}

\begin{document}

% =============================================================================
% Title page
% =============================================================================
\begin{titlepage}
  \thispagestyle{empty}

  \noindent\colorbox{UNSWGold}{\parbox[c][0.35cm][c]{\textwidth}{}}

  \vspace{1.3cm}
  {\Large\bfseries COMP9900 -- Information Technology Project\par}
  \vspace{0.5cm}
  {\huge\bfseries Project 18: Explainable LLMs for\\[0.2cm]
  Mental Health Support\par}
  \vspace{0.8cm}
  {\LARGE\color{Blue}\bfseries Project Report\par}

  \vspace{1.1cm}

  \begin{tabularx}{\textwidth}{@{}L{3.2cm}Y@{}}
    \textbf{Team} & 9900-F09C-ALMOND \\
    \textbf{Client} & Dr Thao Le \\
    \textbf{Submission date} & Week 10 Sunday, 9:00 pm \\
  \end{tabularx}

  \vspace{0.8cm}

  \begin{tabularx}{\textwidth}{@{}L{2.6cm}L{4.8cm}L{1.9cm}Y@{}}
    \toprule
    \textbf{Name} & \textbf{Email} & \textbf{Student ID} & \textbf{Role} \\
    \midrule
    Xi Shen & z5547747@ad.unsw.edu.au & z5547747 & Scrum Master, Backend \\
    Yidi Wu & z5561320@ad.unsw.edu.au & z5561320 & Backend \\
    Jiajun Wang & z5560250@ad.unsw.edu.au & z5560250 & Frontend \\
    Zijie Cai & z5529079@ad.unsw.edu.au & z5529079 & Backend \\
    Meitong Zhou & z5324147@ad.unsw.edu.au & z5324147 & Project Manager, Backend \\
    Jiaxin Liu & z5565763@ad.unsw.edu.au & z5565763 & Backend \\
    \bottomrule
  \end{tabularx}

  \vfill
\end{titlepage}

\pagenumbering{roman}
\tableofcontents
\newpage
\pagenumbering{arabic}

% =============================================================================
% Executive summary
% =============================================================================
\section*{Executive Summary}
\addcontentsline{toc}{section}{Executive Summary}

We built a local web tool for studying how a small open-source language model answers
emotional-reasoning questions from EmoBench \cite{sabour2024}. A user loads a scenario,
sees the model's answer, chooses a different answer (we call it the \emph{foil}), and
runs a search strategy that tries to change the model's answer to that foil by editing
only the scenario text. This follows the general idea of counterfactual explanations:
show the smallest change to the input that changes the output
\cite{wachter2018,miller2019}.

The system contains a simple word-level baseline and three main counterfactual
strategies: \stratone{}, \strattwo{}, and \stratthree{}. All four run against the same
frozen target model through the same verification path, so their results can be
compared fairly.

We do not treat every answer change as a good explanation. The finished system
separates a \emph{mechanical flip} (the model changed its answer) from a
\emph{human-valid explanation} (reviewers agree the edit is a meaningful change), a
\emph{diagnostic finding} (the model reacted to wording, spelling, or a leaked answer
cue), and a valid \texttt{not\_found} result (no flip within the budget). This
distinction became central after our Sprint 2 experiments showed that a higher flip
rate can also mean larger, contradictory, or unhelpful edits.

In Sprint 3 we added a shared minimality stage and a fluency check, built a
discovery-focused variant of each main strategy, clarified the result labels in the
interface, and added accessibility settings. This report explains the final design,
shows the measured results, includes example outputs of good and bad quality, and
states clearly which goals were met and which were not. We present every flip as
evidence of model behaviour under a frozen test setup, not as proof of the model's
internal reasoning.

% =============================================================================
% 1. Installation manual
% =============================================================================
\section{Installation Manual}

The application is dockerised. Docker Compose builds and starts the backend (FastAPI)
and the frontend (React build served by nginx). Ollama stays on the host machine
because it manages the local language models; the project deliberately uses no paid
cloud API. The exact Python, Node, and package versions are pinned in the repository
(\texttt{backend/requirements.txt}, \texttt{frontend/package-lock.json}) and are listed
in the repository README, so the report only describes the steps to run.

\subsection{Prerequisites and Setup}

The machine needs Docker Desktop (or Docker Engine with Compose), Ollama, and the two
EmoBench data files (\texttt{EU.jsonl}, \texttt{EA.jsonl}), which are supplied
separately because the benchmark data is not committed to Git.

\begin{lstlisting}[style=terminal,language=bash]
# 1. Clone the repository and enter it
git clone <team-repository-url>
cd capstone-project-26t2-9900-f09c-almond

# 2. Start Ollama on the host and pull the frozen local model
ollama pull llama3.2:3b

# 3. Place EU.jsonl and EA.jsonl in data/raw/

# 4. Build and start the application
docker compose up --build -d

# 5. Load the EmoBench data into SQLite (one-off)
docker compose exec backend python -m app.scripts.load_emobench \
  --input /data/raw/EU.jsonl
docker compose exec backend python -m app.scripts.load_emobench \
  --input /data/raw/EA.jsonl
\end{lstlisting}

The frontend is then available at \url{http://localhost:5173} and the backend API
documentation at \url{http://localhost:8000/docs}. \texttt{docker compose down} stops
the application; the SQLite database lives in a named volume, so stored experiments
survive restarts. For development without Docker, the README also documents the
manual path (Python 3.11 virtual environment plus \texttt{npm run dev}).

\subsection{Environment Variables and Secrets}

The project needs no secrets: there are no API keys and no cloud services. All
configuration is plain environment variables with working defaults, set in
\texttt{docker-compose.yml} (or copied from \texttt{backend/.env.example} for manual
runs). Table~\ref{tab:env} lists the variables a user may want to change.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{4.2cm}Y L{3.9cm}@{}}
\toprule
\textbf{Variable} & \textbf{Purpose} & \textbf{Release setting} \\
\midrule
\texttt{USE\_MOCK\_LLM} & Deterministic mock mode or real local Ollama & \texttt{false} for real runs \\
\texttt{REPO\_BACKEND} & Memory or SQLite persistence & \texttt{sqlite} \\
\texttt{DATABASE\_URL} & SQLite location & Docker volume path \\
\texttt{OLLAMA\_BASE\_URL} & Local model endpoint & \texttt{http://host.docker.\allowbreak internal:11434} \\
\texttt{DEFAULT\_MODEL} & Frozen target model & \texttt{llama3.2:3b}; digest stored in run metadata \\
\texttt{VITE\_API\_BASE\_URL} & Frontend-to-backend endpoint & \texttt{http://localhost:8000} (build argument) \\
\bottomrule
\end{tabularx}
\caption{Environment configuration. No secrets are required.}
\label{tab:env}
\end{table}

\subsection{Clean-Machine Verification}

We verified the dockerised installation on a Windows 11 machine with Docker Desktop by
following exactly the steps above from a clean checkout: both images built, both
containers started, the backend health endpoint reported real (non-mock) mode, both
EmoBench files loaded into SQLite, one real prediction ran through the containerised
backend against the host Ollama model, and the containers were restarted without
losing stored results. The tested Docker, Ollama, and model digest values are recorded
in the repository README.

% =============================================================================
% 2. Architecture
% =============================================================================
\section{System Architecture Diagram}

Figure~\ref{fig:architecture} shows the completed architecture. There is one frozen
target path for every strategy, and one shared quality pipeline after strategy search,
so minimality, fluency, risk labels, and metrics are computed the same way for every
result.

\begin{figure}[htbp]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=0.62cm and 1.6cm,
  block/.style={
    draw=Navy,
    rounded corners=2mm,
    fill=PaleBlue,
    align=center,
    minimum width=5.0cm,
    minimum height=0.95cm,
    font=\small
  },
  quality/.style={
    block,
    fill=PaleGold
  },
  source/.style={
    block,
    fill=PaleGrey
  },
  flow/.style={-{Latex[length=2.5mm]},thick,draw=Navy},
  dataflow/.style={-{Latex[length=2.5mm]},thick,draw=Blue,dashed}
]
  \node[block] (ui) {React UI\\Experiment, Comparison, Settings};
  \node[block, below=of ui] (api) {FastAPI Routes\\request validation and response mapping};
  \node[block, below=of api] (services) {Service Layer\\prediction, counterfactual, evaluation};
  \node[block, below=of services] (strategies) {Strategy Layer\\Baseline, Strategy 1, Strategy 2, Strategy 3};
  \node[quality, below=of strategies] (quality) {Shared Quality Pipeline\\minimality, fluency, risk labels};
  \node[block, below=of quality] (results) {Results and Metrics\\valid, diagnostic, not found, failed};

  \node[source, left=of services] (sqlite) {SQLite\\scenarios, cache, jobs, results};
  \node[source, left=of sqlite] (emobench) {EmoBench\\local dataset};
  \node[block, right=of strategies] (target) {Frozen Target Harness\\foil-blind verification};
  \node[source, right=of target] (ollama) {Local Ollama\\target, proposer, and judge models};

  \draw[flow] (ui) -- node[right,font=\scriptsize]{REST/JSON} (api);
  \draw[flow] (api) -- (services);
  \draw[flow] (services) -- (strategies);
  \draw[flow] (strategies) -- (quality);
  \draw[flow] (quality) -- (results);
  \draw[dataflow] (emobench) -- node[above,font=\scriptsize]{one-off import} (sqlite);
  \draw[dataflow] (sqlite) -- node[above,font=\scriptsize]{read/write} (services);
  \draw[flow] (strategies) -- node[above,font=\scriptsize]{candidate verification} (target);
  \draw[flow] (quality) -| node[near start,below,font=\scriptsize]{re-verification} (target);
  \draw[flow] (target) -- (ollama);
  \draw[dataflow] (results.west) -| (sqlite.south);
\end{tikzpicture}%
}
\caption{Final architecture. The Strategy Layer contains the word-level Baseline and
the three main strategies; all of them verify candidates only through the Frozen
Target Harness.}
\label{fig:architecture}
\end{figure}

\subsection{Major Components and Data Flow}

\begin{enumerate}
  \item The user selects an EmoBench scenario and the local model in the React
  interface.
  \item The backend asks the frozen target harness for the original answer. The target
  prompt is stateless and deterministic, and it never contains the foil.
  \item The user selects a foil and one strategy (Baseline, Strategy 1, Strategy 2, or
  Strategy 3, each with a strict or discovery variant where available).
  \item The strategy proposes scenario edits in its own way. Only the frozen target
  decides whether a candidate reaches the foil.
  \item A successful candidate then passes through shared minimality, fluency scoring,
  and risk labelling before metrics are stored and displayed.
  \item A search that uses up its budget without a flip is stored as
  \texttt{not\_found}. It is not turned into an error, and no explanation is invented.
\end{enumerate}

Because every strategy shares the same target function, prompt version, success rule,
budgets, and metric definitions, differences between strategies can only come from how
they search. That is what makes the comparison fair.

% =============================================================================
% 3. Design justifications
% =============================================================================
\section{Design Justifications}

We treated the design as a series of decisions driven by experiments and by client and
tutor feedback, rather than a fixed feature list. Table~\ref{tab:evolution} summarises
the main changes across the three sprints.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{1.7cm}Y Y Y@{}}
\toprule
\textbf{Stage} & \textbf{Initial focus} & \textbf{Evidence or feedback} & \textbf{Design response} \\
\midrule
Sprint 1 & Deliver an end-to-end local MVP and a first mechanical strategy & A model flip is not interpretable unless every check uses the same target function & Freeze the target harness, add a shared strategy interface, and treat \texttt{not\_found} as a valid result \\
Sprint 2 & Add the three main strategies, persistence, and fair comparison & More candidates increased flips, but some edits were large, contradictory, unnatural, or leaked the answer & Add event grounding, guards, diagnostics, reproducible comparison, and provisional human validity review \\
Sprint 3 & Improve explanation quality and accessibility & Strict per-strategy word limits rejected useful candidates; flip rate alone hid model sensitivities and invalid explanations & Let strategies focus on discovery, then apply shared minimality and fluency, and separate valid explanations from diagnostic findings \\
\bottomrule
\end{tabularx}
\caption{Design evolution from the Sprint 1 MVP to the completed Sprint 3 system.}
\label{tab:evolution}
\end{table}

\subsection{Terminology in Plain Language}
\label{sec:terms}

These terms are used throughout the report.

\begin{itemize}
  \item \textbf{Foil.} The alternative answer the user wants the model to give. The
  strategy is allowed to know it; the target model is never shown it.
  \item \textbf{Mechanical flip.} After an edit, the frozen target model's answer
  equals the foil exactly. This only says the answer changed, not that the edit is a
  good explanation.
  \item \textbf{Minimality.} How small the edit is. We measure it as the word-level
  edit distance between the original and the edited scenario: the number of single-word
  insertions, deletions, and replacements needed to turn one into the other. We also
  record the changed-word fraction, which is that count divided by the scenario length
  in words. Smaller is better, because a small change is easier for a person to read
  and reason about.
  \item \textbf{Fluency.} Whether the edited text still reads as natural English. We
  score it automatically with a local judge model (Section~\ref{sec:quality}) and treat
  the score as a proxy, not proof.
  \item \textbf{Semantic validity.} Whether the edit is a meaningful change to the
  situation (an event, outcome, relationship, or behaviour) rather than a trick such as
  copying the answer into the text, swapping a word for a synonym, or contradicting the
  rest of the story. Our code flags obvious cases, but the final judgement needs human
  reviewers, who label each flip Good, Acceptable, or Invalid with reason codes.
  \item \textbf{Diagnostic finding.} A verified flip that our checks classify as a
  model sensitivity (for example a near-synonym swap or an answer leak). We keep these
  as evidence about the model, but never publish them as explanations.
  \item \textbf{Budget.} The maximum number of target-model checks one strategy may
  use for one scenario. Search and post-processing each have a capped allowance, and
  all counts are recorded, so no strategy can look cheap by hiding work.
\end{itemize}

\subsection{Frozen Target Harness}

The project explains a fixed black-box model. Every strategy calls the same target
prompt, decoding configuration, answer parser, and cache contract. If each strategy
could change the target prompt, differences between results could come from the prompt
instead of the edit, and the comparison would be meaningless. The full target prompt is
in Appendix~\ref{app:prompts}. A flip is exactly
\texttt{prediction.answer == foil}.

\subsection{Strategy Families}
\label{sec:strategies}

The client asked us to number the strategies clearly, so the report uses Strategy 1--3
plus the Baseline. Appendix~\ref{app:mapping} maps these names to the identifiers used
in the code and in stored experiment metadata. Figure~\ref{fig:flows} shows the search
flow of each strategy.

\paragraph{Baseline: word-level greedy search.}
The Baseline systematically tries small word and phrase substitutions from a fixed
list and verifies each attempt. It uses no LLM to generate candidates. It is
transparent and cheap, but it cannot express a larger event change, so it mainly
serves as a reference point.

\paragraph{\stratone{}.}
A local proposer LLM reads the scenario, the choices, and the foil, and proposes
complete rewritten scenarios that change one event, outcome, relationship, or
observable behaviour. Deterministic guards reject rewrites that break structure, leak
the answer, or change too much, and the frozen target verifies the survivors. The
Sprint 2 version (\emph{strict}) prefers about three and at most six changed words.
The Sprint 3 \emph{discovery} variant removes the fixed word cap, keeps a
changed-fraction safety cap of 0.60, retains up to three distinct verified flips, and
sends the best one to the shared quality pipeline. This follows the zero-shot
LLM-counterfactual direction of \cite{bhattacharjee2024} while keeping our target
model and verification frozen.

\paragraph{\strattwo{}.}
This strategy first estimates which parts of the text matter. It occludes (masks) one
span at a time, asks the frozen target how its confidence in the answers changes, and
ranks spans by importance; missing confidence values fall back to an answer-change
heuristic and the coverage is reported rather than silently filled with zeros. It then
asks the proposer LLM to infill the most important spans with new content and verifies
the results. The idea adapts importance-guided contrastive editing \cite{ross2021} to
a black-box setting without gradients. The discovery variant splits the fixed budget
between importance probes and candidate checks, explores several top spans and mask
widths, and keeps diagnostic candidates separate.

\paragraph{\stratthree{}.}
This strategy changes exactly one situational concept, such as the time, place,
relationship, urgency, or outcome. The proposer LLM must point to one exact text span
and give a replacement for that span; our code checks the span occurs exactly once and
applies the replacement deterministically, so the LLM never rewrites the whole story.
It is inspired by concept-level counterfactual work \cite{gat2024}. We describe it as
\emph{causal-inspired}: changing one concept and observing the model is an
intervention on the text, not proof of real-world causality. The discovery variant
adds an outcome concept class, allows a coherent multi-word replacement span, and
demotes overbroad spans (at least 12 tokens and more than half the scenario) to
diagnostic findings.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  sbox/.style={draw=Navy,rounded corners=1.5mm,fill=PaleBlue,align=center,
    font=\scriptsize,minimum height=0.72cm,inner sep=3pt},
  tbox/.style={sbox,fill=PaleGold},
  lbl/.style={font=\scriptsize\bfseries,anchor=west},
  arr/.style={-{Latex[length=2mm]},thick,draw=Navy}
]
  % Baseline
  \node[lbl] (l0) at (0,0) {Baseline};
  \node[sbox,right=0.32cm of l0] (b1) {pick next word/phrase\\substitution from fixed list};
  \node[sbox,right=0.5cm of b1] (b2) {apply to scenario};
  \node[tbox,right=0.5cm of b2] (b3) {frozen target check};
  \node[sbox,right=0.5cm of b3] (b4) {flip? publish\\else next candidate};
  \draw[arr] (b1)--(b2); \draw[arr] (b2)--(b3); \draw[arr] (b3)--(b4);

  % S1
  \node[lbl,below=1.05cm of l0.west,anchor=west] (l1) {Strategy 1};
  \node[sbox,right=0.32cm of l1] (s11) {LLM proposes full\\rewrites (sees foil)};
  \node[sbox,right=0.5cm of s11] (s12) {guards: structure,\\leak, changed fraction};
  \node[tbox,right=0.5cm of s12] (s13) {frozen target check\\(never sees foil)};
  \node[sbox,right=0.5cm of s13] (s14) {keep up to 3 flips;\\best to quality stage};
  \draw[arr] (s11)--(s12); \draw[arr] (s12)--(s13); \draw[arr] (s13)--(s14);

  % S2
  \node[lbl,below=1.05cm of l1.west,anchor=west] (l2) {Strategy 2};
  \node[sbox,right=0.32cm of l2] (s21) {occlude spans;\\rank importance};
  \node[sbox,right=0.5cm of s21] (s22) {LLM infills\\important spans};
  \node[tbox,right=0.5cm of s22] (s23) {frozen target check};
  \node[sbox,right=0.5cm of s23] (s24) {primary vs diagnostic;\\best to quality stage};
  \draw[arr] (s21)--(s22); \draw[arr] (s22)--(s23); \draw[arr] (s23)--(s24);

  % S3
  \node[lbl,below=1.05cm of l2.west,anchor=west] (l3) {Strategy 3};
  \node[sbox,right=0.32cm of l3] (s31) {LLM proposes one\\concept span edit};
  \node[sbox,right=0.5cm of s31] (s32) {code grounds span;\\applies edit itself};
  \node[tbox,right=0.5cm of s32] (s33) {frozen target check};
  \node[sbox,right=0.5cm of s33] (s34) {single-concept guards;\\best to quality stage};
  \draw[arr] (s31)--(s32); \draw[arr] (s32)--(s33); \draw[arr] (s33)--(s34);

  % Shared pipeline
  \node[lbl,below=1.15cm of l3.west,anchor=west] (l4) {Shared stages};
  \node[sbox,right=0.32cm of l4,fill=PaleGrey] (q1) {verified raw flip};
  \node[sbox,right=0.5cm of q1,fill=PaleGrey] (q2) {minimality:\\greedy restore};
  \node[sbox,right=0.5cm of q2,fill=PaleGrey] (q3) {fluency score;\\one bounded polish};
  \node[sbox,right=0.5cm of q3,fill=PaleGrey] (q4) {risk flags;\\human review};
  \draw[arr] (q1)--(q2); \draw[arr] (q2)--(q3); \draw[arr] (q3)--(q4);
\end{tikzpicture}
\caption{Search flow of each strategy. Yellow boxes are frozen target checks, which
are the only way any strategy can test a candidate. Every verified flip then enters
the same shared quality stages (bottom row), each of which re-verifies through the
same frozen target.}
\label{fig:flows}
\end{figure}

\subsection{Shared Quality Pipeline: How Each Stage Works}
\label{sec:quality}

Our proposal separated searching for a flip from judging its quality, and Sprint 3
completed that design. The stages run in a fixed order, and every accepted
transformation is re-verified by the frozen target.

\paragraph{Minimality (greedy restoration).}
The minimiser takes the verified edited scenario and tries to shrink the edit. It
lines up the original and edited text, finds the changed regions, and then repeatedly
tries to restore original content, first clause by clause, then word by word. After
each candidate restoration it asks the frozen target again. If the answer is still the
foil, the restoration is kept; if the flip is lost, the restoration is rolled back.
It stops when nothing more can be restored or when the post-processing call cap is
reached. The result is a verified local minimum, not a guaranteed global minimum. We
record the edit distance before and after, and both texts stay visible, because a
smaller edit can hide that the original mechanism was weak.

\paragraph{Fluency (scored proxy).}
A separate local judge (the same \texttt{llama3.2:3b} model with a fixed rubric
prompt, temperature 0, prompt version \texttt{fluency-judge-v2-rubric-100}) reads only
the edited scenario, with no choices and no foil, and returns an integer from 0 to
100: 90--100 natural, 75--89 minor issue, 40--74 noticeably awkward, 0--39 broken. We
store the score divided by 100, so the final fluency score is
$\text{score} = \text{judge integer} / 100 \in [0,1]$, and a result passes when
$\text{score} \ge 0.75$. If the judge fails or returns invalid output, we store
\texttt{not\_measured} with a reason instead of inventing a value. The score is a
proxy: Section~\ref{sec:qualitative} shows a broken sentence that still passed, so we
never present a fluency pass as proof of grammar.

\paragraph{One constrained polish.}
If the fluency score is low, the pipeline may attempt exactly one polish. The
constraints are strict: the polish may only delete unnecessary words from the changed
span, keeping the remaining words in their original order; it must not add, replace,
reorder, or inflect words, must not increase the edit distance, must not change
sentence boundaries, and must not introduce option letters, answer wording, or emotion
labels. The polished text must still score higher than before and must still flip the
frozen target, otherwise the polish is rolled back and the pre-polish result stands.

\paragraph{Risk flags and human review.}
Deterministic checks flag high-confidence problems: exact or morphological answer
leakage, option references such as ``answer is C'', near-synonym-only or
surface-form-only changes, focal-person shifts, multi-concept spans, and overbroad
concept spans. Flagged flips become diagnostic findings. Everything that survives is
still only ``mechanical flip, review needed'' until two reviewers independently label
it Good, Acceptable, or Invalid with reason codes, hidden from each other, with
disagreements adjudicated. Minimality and fluency cannot rescue an invalid edit: a
short, grammatical answer leak is still invalid.

% =============================================================================
% 4. User-driven evaluation
% =============================================================================
\section{User-Driven Evaluation of the Solution}

\subsection{Main User Workflow and Features}

The completed system supports the following workflow:

\begin{enumerate}
  \item load an EmoBench scenario and view all its answer choices;
  \item run the frozen local model and view its original answer;
  \item choose a non-original foil answer;
  \item run the Baseline, Strategy 1, Strategy 2, or Strategy 3 as an asynchronous
  job with a chosen budget;
  \item inspect the original, raw, and final scenarios side by side with red deletion
  and green insertion highlighting, plus status, call counts, edit metrics, fluency,
  risk flags, and result labels;
  \item compare several strategies on one scenario or on a fixed batch under the same
  model, question set, foil plan, and budget, including the frozen development and
  holdout ID lists built into the Comparison page;
  \item adjust language (English or Simplified Chinese), font size (100--150\%),
  light/dark/system theme, reduced motion, and a focus magnifier in Settings.
\end{enumerate}

We tested this workflow with keyboard-only navigation, browser zoom at 200\%, every
font scale, both interface languages, both themes, and reduced motion. Colour is
always paired with a text label. Switching the interface language never translates or
changes the scenario and choices sent to the model, because that would change the
input being evaluated.

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][3.2cm][c]{0.88\textwidth}{\centering
\textbf{INSERT FINAL UI SCREENSHOTS}\\[0.3cm]
Experiment page: one reviewed result with highlighted edits.\\
Comparison page: the same scenario, foil, model, and budget across strategies.}}
\caption{Experiment and Comparison workflows of the completed system.}
\label{fig:screens}
\end{figure}

\subsection{Evaluation Objectives and Criteria}

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{2.7cm}Y Y@{}}
\toprule
\textbf{Objective} & \textbf{Measure} & \textbf{Interpretation} \\
\midrule
Functional workflow & Completion of load, predict, foil, search, result, and comparison tasks & Confirms the tool supports the intended research workflow \\
Flip discovery & Raw mechanical flip rate and \texttt{not\_found} rate & Measures search coverage, not explanation validity \\
Minimality & Raw/final edit distance, changed-word fraction, one-to-two-word rate & Measures how much text must change while the flip remains \\
Fluency & Versioned automatic score plus human judgement & Finds readability problems; the automatic score is not proof \\
Semantic validity & Two-reviewer Good/Acceptable/Invalid labels with reason codes & Separates useful explanations from leakage, contradiction, synonym, and surface effects \\
Efficiency & Search, post-processing, proposer, and total calls; same-machine runtime & Shows the local CPU cost of each strategy and quality stage \\
Usability and accessibility & Keyboard completion, contrast, zoom, themes, language, labels & Whether users can operate and understand the results \\
Reproducibility & Fixed commit, model digest, prompt versions, seed, IDs, foil plan, budgets & Makes comparisons auditable and repeatable \\
\bottomrule
\end{tabularx}
\caption{Evaluation criteria used in Sprint 3.}
\label{tab:criteria}
\end{table}

\subsection{Result Categories}

The application separates automated execution outcomes from later human annotation.
Every completed run returned by the current API is reported as exactly one of four
automated categories:

\begin{itemize}
  \item \textbf{Mechanical flip -- review needed:} the target reached the foil, but
  quality has not been established yet;
  \item \textbf{Diagnostic finding:} a verified flip caused by lexical, formatting,
  focal-shift, evaluative-cue, or answer-leakage sensitivity and retained as evidence
  rather than a publishable counterfactual;
  \item \textbf{Not found:} no flip within the frozen budget, which is a real research
  result about model stability;
  \item \textbf{Failed:} an infrastructure or execution error, reported separately
  from bounded-search outcomes and kept visible in coverage reporting.
\end{itemize}

\emph{Valid explanation} is deliberately not an automatic application outcome. Human
review is conducted outside the web application using the frozen rubric and blinded
review sheets. Reviewers label a mechanical flip Good, Acceptable, or Invalid; only
Good and Acceptable labels count as human-valid in the external evaluation artifacts.
The current API, database schema, and interface do not store reviewer identities,
adjudication state, or a final human-valid category.

\subsection{Sprint 2 Evidence Used During Design}

Table~\ref{tab:prelim} records the Sprint 2 evidence that motivated the Sprint 3
design. The rows come from different samples, so we do not rank them against each
other, and the human labels were provisional single-reviewer triage.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{3.4cm}L{2.8cm}L{2.2cm}Y@{}}
\toprule
\textbf{Evidence} & \textbf{Sample} & \textbf{Mechanical flips} & \textbf{Quality note} \\
\midrule
Strategy 1, early broad prompt (v2) & EU-400 & 71/400 & Broad coverage but average edit distance about 16.7 words; validity uncertain \\
Strategy 1, constrained (v3) & EU-400 & 15/400 & Average edit distance 3.9; the strict word cap cost most of the coverage \\
Strategy 1, prompt ablation (0/1/3 examples vs v5) & Success-enriched dev-40 & 10 / 14 / 2 vs 8 & More flips did not mean better explanations; the three-example drop has no confirmed cause \\
Strategy 1, hardened v5 & Success-enriched dev-40 & 5/40 & Single-reviewer triage found 1/5 provisionally acceptable \\
Strategy 1, frozen holdout v2 & Holdout-20 & 2/20 & One provisionally acceptable, one provisionally invalid \\
Strategy 3, concept-guided & EU-400 & 39/400 & Average successful edit distance 4.97; human validity not assessed in Sprint 2 \\
\bottomrule
\end{tabularx}
\caption{Historical Sprint 2 evidence with non-comparable samples and provisional labels.}
\label{tab:prelim}
\end{table}

\subsection{Sprint 3 Results: Strategy 1 Strict versus Discovery}
\label{sec:s1results}

We froze the configuration and ran the Sprint 2 strict variant and the Sprint 3
discovery variant of Strategy 1 in one paired job on the same fixed 40 development
questions, same model (\texttt{llama3.2:3b}, pinned digest), same seed, and the same
search budget of 20 target calls, with shared minimality and fluency enabled. This
set is success-enriched (questions where earlier versions had found flips), so the
rates are for comparing the two variants, not population estimates. All 80 units
completed with no failures.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}Y r r@{}}
\toprule
\textbf{Metric} & \textbf{Sprint 2 strict} & \textbf{Sprint 3 discovery} \\
\midrule
Primary review-candidate scenarios & 4/40 (10.0\%) & 10/40 (25.0\%) \\
Total raw verified flips & 4 & 14 \\
Diagnostic-only scenarios & 0/40 & 0/40 \\
Average raw edit distance on flips (words) & 4.00 & 9.40 \\
Average final edit distance after minimality & 1.50 & 4.10 \\
Average target calls per scenario & 1.95 & 4.55 \\
Automatic fluency passes on final results & 4/4 & 9/10 \\
\bottomrule
\end{tabularx}
\caption{Paired Strategy 1 comparison on the fixed dev-40 set (budget 20, seed 0).}
\label{tab:s1}
\end{table}

Discovery found six more review-candidate scenarios (25\% versus 10\%), which is what
it was designed for: broader search first, quality processing afterwards. The cost is
also visible: discovery made larger raw edits (9.40 versus 4.00 words) and used more
than twice the target calls (4.55 versus 1.95). Minimality then reduced both variants'
edits by more than half. The candidate funnel explains where proposals go: Discovery
requested 320 candidates, received 194 raw items, parsed 193, and delivered 178 after
applying the requested-count limit. Of these, 64 were unique valid primary candidates
and 63 reached target verification; 14 verified candidates flipped across 10
scenarios. The main structural rejection was failure to preserve sentence structure
(94 proposals), and eight proposals exceeded the changed-fraction cap. Human review
of these ten candidates is still in progress, so we report them as mechanical flips
awaiting review, not as valid explanations.

\subsection{Sprint 3 Results: Strategy 3 Strict versus Discovery}
\label{sec:s3results}

For Strategy 3 we froze a new evaluation set of 20 questions before looking at any of
its scenarios or outputs. The set is disjoint from all earlier Strategy 3 work and all
reviewed IDs; we disclose that an early Strategy 1 sweep had once processed all 400 EU
questions, so no set can be called globally untouched. Both variants ran once in one
paired job at commit \texttt{7fcdc82} with search and post-processing caps of 20 calls
each; the selection hashes, artifact checksum, and full configuration are recorded in
the repository evaluation documents.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}Y r r@{}}
\toprule
\textbf{Metric} & \textbf{Sprint 2 strict} & \textbf{Sprint 3 discovery} \\
\midrule
Primary mechanical flips retained for review & 0/20 (0\%) & 1/20 (5\%) \\
Verified diagnostic flips & 0 & 0 \\
\texttt{not\_found} & 20/20 & 19/20 \\
Failed or skipped & 0 & 0 \\
Average total target calls & 2.40 & 2.15 \\
Average proposer calls & 2.50 & 2.30 \\
Average runtime per scenario (same machine) & 15.81 s & 11.35 s \\
Final edit distance on the flip & -- & 7 \\
Fluency score on the flip & -- & 0.90 \\
\bottomrule
\end{tabularx}
\caption{Paired Strategy 3 comparison on the frozen evaluation-20 set.}
\label{tab:s3}
\end{table}

Both variants were mostly insensitive on this harder, unenriched set: strict found
nothing and Discovery found one flip. The Discovery variant also generated 14
diagnostic candidates (10 focal shifts, 5 downstream references, 4 multi-concept, 1
evaluative cue, 1 foil leak, and 1 near-synonym). A candidate may carry more than one
risk flag, so these flag counts are not mutually exclusive. None of the diagnostic
candidates flipped the target. They were handled separately from primary candidates
and verified only with target-call budget remaining after the primary search. On the
one primary flip, minimality spent 15 post-processing calls, could not
find a smaller verified version that preserved the concept metadata, and correctly
kept the raw verified edit. The flip itself is discussed in
Section~\ref{sec:qualitative}; the coordinator's provisional view is that it is
invalid as an explanation but useful as a sensitivity finding, and two independent
blinded labels are still pending. On this evidence, discovery stays experimental and
does not replace strict as the default.

\subsection{Human Review Evidence}
\label{sec:review}

Blinded dual review is an offline evaluation protocol rather than a feature of the web
application. We exercised that protocol fully once during Sprint 3 using blinded
workbooks for an earlier Strategy 3 run. That run was later found to have a
contaminated control arm (the strict arm had received part of the Discovery
configuration), so its strict-versus-Discovery comparison is void and the 20 questions
are consumed; we keep the run only as review-process evidence and repeated the
mechanical comparison on the new frozen set above. Two reviewers independently
labelled all four published flips with strategy identity hidden, agreed on 3 of 4
validity labels, and adjudicated the one disagreement. The final labels were 4 Invalid
out of 4, with reason codes including grammar, irrelevant change, near-synonym,
contradiction, evaluative cue, excessive edit, and model sensitivity. The reviewer
sheets and reconciled labels are external evaluation artifacts; they are not persisted
by the current application.

This is an informative negative result. All four edits had exact single-concept
metadata, verified flips, passing fluency scores, and moderate edit sizes, and human
reviewers still judged all four invalid as explanations. Mechanical checks alone are
not enough, which is why the evaluation methodology requires a separate human-review
stage.

\subsection{Strategy 2 Status}

Strategy 2's Discovery variant is implemented and covered by dedicated tests for its
budget split, probe accounting, and diagnostic separation. The complete backend suite
currently contains 230 passing tests. A three-question real-model smoke run confirmed
that both variants completed correctly and found 0/3 flips. We deliberately do not
present those three questions as effectiveness evidence. A full paired development and
evaluation run like
Sections~\ref{sec:s1results} and \ref{sec:s3results} was not completed within the
term, so Strategy 2's comparative effectiveness is an open objective, and we list it
in the limitations.

\subsection{Runtime}
\label{sec:runtime}

Full-dataset runtime is long on CPU (a 400-question batch takes hours), so following
the client's suggestion we report one selected high-runtime example: the question with
the longest observed runtime in the retained Sprint 3 artifacts available when this
report was prepared
(\texttt{emobench\_q\_en\_121\_emotion}, 44 words, previously 59.6 s under Strategy 1
discovery). We ran all seven strategy arms on it once, sequentially, on the same
machine (Windows 11, CPU-only, \texttt{llama3.2:3b}, search budget 20, single foil,
shared quality stages enabled). The raw timing artifact is retained outside Git and
must be included in the handover package with its file name and checksum; this table is
descriptive evidence, not a benchmark over the full dataset.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}Y r r r r@{}}
\toprule
\textbf{Strategy arm} & \textbf{Runtime} & \textbf{Outcome} & \textbf{Target calls} & \textbf{Proposer calls} \\
\midrule
Baseline (word-level greedy) & 17.5 s & flip & 6 & 0 \\
Strategy 1 strict & 19.0 s & flip & 5 & 2 \\
Strategy 1 discovery & 6.8 s & not found & 0 & 2 \\
Strategy 2 strict & 62.2 s & not found & 17 & 13 \\
Strategy 2 discovery & 22.6 s & not found & 10 & 6 \\
Strategy 3 strict & 17.1 s & flip & 9 & 1 \\
Strategy 3 discovery & 6.1 s & not found & 1 & 2 \\
\bottomrule
\end{tabularx}
\caption{Selected high-runtime single-question execution across all strategy arms
(same machine, budget 20). Target calls include search and post-processing checks.}
\label{tab:runtime}
\end{table}

Two things are visible. First, Strategy 2 strict is the most expensive arm (62 s),
because occlusion probing spends many target calls before any candidate exists; this
matches its design. Second, runtime depends strongly on when a flip is found: arms
that flipped early stopped early, and \texttt{not\_found} arms either exhausted their
proposals or their probes. Because the proposer runs at temperature 0.7 and is not
bit-for-bit repeatable across runs, and this is one question, the table illustrates
cost scale only; it is not a ranking claim.

\subsection{Qualitative Analysis: Good and Bad Examples}
\label{sec:qualitative}

Numbers alone hide what the edits actually look like, so here are real outputs, quoted
from stored artifacts, with our assessment.

\paragraph{Provisionally acceptable (Strategy 1, holdout question 107).}
The original scenario ends ``\ldots the result was \emph{the expected one}.'' The edit
replaces this with ``\ldots the result was \emph{unusually accurate}.'' The model's
answer changed to the foil. The edit changes the real outcome of the event, stays
grammatical, and does not mention any answer text, so the reviewer marked it
provisionally acceptable. This is the behaviour we want the tool to find.

\paragraph{Invalid: surface-level colour change and model sensitivity (Strategy 1,
holdout question 132).}
The edit changed one word, ``pale'', to ``green'', in a description of someone's face.
The model flipped, but no fact about the situation changed. This is lexical
instability of the model, so the provisional human review marked it Invalid with a
model-sensitivity reason. This historical strict-strategy result predates the
Discovery diagnostic queue, so we do not claim that the application automatically
classified it as a diagnostic outcome.

\paragraph{Invalid: cue made more explicit (Strategy 3 discovery, evaluation
question 162).}
The edit replaced ``his face squished up'' with ``a look of distaste on his face'',
and the model moved from Embarrassment to Annoyance. The automatic checks flagged
nothing and fluency scored 0.90, but content review found the modified clause is a
sentence fragment, the ``emotional intensity'' concept label does not match what the
replacement actually does, and the change mainly makes an already negative cue more
explicit rather than changing the situation. Provisionally invalid; blinded labels
pending. It is still useful evidence that the model is sensitive to the explicit word
``distaste''.

\paragraph{Quality issue flagged by the fluency proxy (Strategy 1 discovery, dev
question 161).}
Discovery found a coherent raw rewrite: ``It said there is a restaurant famous for its
rude customer service'' became ``She had already heard about the infamous waiters who
often insult customers''. Minimality then reduced the 12-word edit to 2 words, leaving
``She said already is a restaurant famous\ldots'', which is broken English. The
fluency judge scored it 0.62, below the 0.75 threshold, so the quality issue was
flagged and reported. A low score does not erase the verified mechanical result; it
marks the result for cautious interpretation and possible bounded polishing. This
example is why we keep the raw, minimal, and final versions all visible.

\paragraph{Quality issue missed by the fluency proxy (Strategy 1 discovery, dev
question 121).}
On another question, minimisation of a larger rewrite produced ``\ldots Vivian's leg
began shake uncontrollably'', which drops the word ``to'' and is ungrammatical, yet
the judge scored it 0.83 and passed it. This false pass is direct evidence that the
automatic fluency score is a proxy that can be wrong in both directions, and it is one
reason human review remains mandatory.

\subsection{Which Objectives Were Met}

Against the criteria in Table~\ref{tab:criteria}, the functional experiment and
comparison workflow, automated outcome categories, flip discovery, minimality
processing, fluency measurement, and efficiency metrics are implemented. The
interface also implements Simplified Chinese, font scaling, system/light/dark themes,
reduced-motion handling, enhanced focus, and a magnifier. These are implementation
outcomes; the repository does not contain a formal frontend usability or accessibility
test suite, so they are not presented as a completed user-study result.

Reproducibility is supported by frozen question-ID lists, evaluation documents,
checksums, Git commits, and retained external artifacts. However, the generic
experiment-run record does not automatically store every model digest, seed, Ollama
version, proposer prompt, or machine detail. Fluency is measured, but its reliability
is only partly established: one false pass and one correct flag are documented above.
Semantic validity remains the least complete objective. The offline dual-review
protocol was completed for one consumed run, while the newest Strategy 1 and Strategy
3 labels are pending. Strategy 2 has smoke-level execution evidence but no full paired
evaluation, and the current application does not persist reviewer labels or
adjudication outcomes.

% =============================================================================
% 5. Limitations and future work
% =============================================================================
\section{Limitations and Future Work}

\subsection{Limitations}

\begin{itemize}
  \item \textbf{Small local model.} The default 3B model runs on CPU laptops but has
  limited emotional reasoning, instruction following, and editing ability. Observed
  sensitivities may be specific to this model.
  \item \textbf{Black-box evidence.} A counterfactual shows the output changed when
  the input changed. It does not reveal internal reasoning or prove causality, and we
  label Strategy 3 causal-inspired for exactly this reason.
  \item \textbf{Mechanical versus valid flips.} Minimality and fluency cannot turn an
  answer leak, contradiction, or irrelevant cue into a valid explanation. On the one
  fully reviewed run, all four published flips were judged invalid.
  \item \textbf{Human review scale.} Two-reviewer validity labels exist for one
  consumed run; the blinded labels for the newest Strategy 1 and Strategy 3 flips are
  still pending, so all newer validity statements are provisional.
  \item \textbf{Automatic quality proxies.} The fluency judge false-passed a broken
  sentence (0.83) and the risk rules flagged none of the four human-invalid flips, so
  neither replaces review.
  \item \textbf{Minimality side effects.} Greedy restoration sometimes removes
  connective words and produces fragments; raw and final texts are both kept visible
  to compensate.
  \item \textbf{Incomplete comparisons.} Strategy 2 has only smoke-level evidence, and
  a single all-strategy controlled holdout matrix was not run within the term; the
  per-strategy paired comparisons above use different sets and must not be compared
  against each other.
  \item \textbf{Run variability.} The proposer runs at temperature 0.7 and is not
  bit-for-bit repeatable across local runs even with a fixed seed, so paired
  same-job comparisons are reported and repeated-run variance remains unmeasured.
  \item \textbf{Evaluation scope.} EmoBench is a benchmark, not a clinical setting.
  The tool analyses model behaviour and must not be used to provide mental-health or
  medical advice.
  \item \textbf{Language setting.} The interface language switch translates the
  interface only; translating scenarios would change the input being evaluated.
\end{itemize}

\subsection{Client Handover}

The handover package contains:

\begin{itemize}
  \item the team GitHub repository at the frozen release commit, with CI passing;
  \item the dockerised installation path in this report and the pinned-version README;
  \item architecture, strategy-interface, evaluation-protocol, and rubric documents in
  \texttt{docs/}, including the frozen question-ID lists and artifact checksums that
  make every reported table reproducible;
  \item the raw experiment artifacts (JSON exports and SQLite databases) kept outside
  Git, with their SHA-256 checksums recorded in the tracked documents;
  \item a live walkthrough of the Experiment, Comparison, and Settings pages showing
  one valid, one diagnostic, and one not-found outcome;
  \item this limitations and future-work section, which identifies the experimental
  components (discovery variants, fluency judge) that are research tools rather than
  finished features.
\end{itemize}

\begin{figure}[htbp]
\centering
\fbox{\parbox[c][2.8cm][c]{0.88\textwidth}{\centering
\textbf{INSERT HANDOVER COMMUNICATION SCREENSHOT}\\[0.3cm]
Screenshot of the handover message to the client covering the repository and release,
documentation, demonstration details, and known limitations.}}
\caption{Evidence of client handover communication.}
\label{fig:handover}
\end{figure}

% NOTE FOR THE TEAM: insert the real screenshot above once the handover message is
% sent. If it is sent before submission, delete the pending sentence below.
The live walkthrough and the final handover message are scheduled together with the
Week 10 demonstration session. If any handover step is still pending at submission
time, that step and the reason are stated here, and the final communication
screenshot will be provided to the tutor as soon as it is sent.

\subsection{Future Work}

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{3.1cm}Y Y@{}}
\toprule
\textbf{Future direction} & \textbf{Why it is useful} & \textbf{Resources required} \\
\midrule
Complete the evaluation programme & Finishes the pending blinded labels, the Strategy 2 paired runs, and one all-strategy holdout matrix & Reviewer time, a new frozen question set, and repeated benchmark runs \\
Larger or multiple local models & Tests whether observed sensitivities are model-specific & More RAM/GPU time, pinned models, repeated runs \\
Broader independent annotation & Produces stronger validity and fluency estimates & More reviewers, the existing protocol, adjudication time \\
Validated semantic and fluency evaluators & Reduces manual triage without hiding uncertainty & Labelled calibration data and possibly a separate local judge model \\
User study with researchers & Tests whether the labels support real analysis tasks, in the spirit of interpretability tooling \cite{tenney2020} & Ethics review, participants, task design, analysis time \\
More counterfactual families and tasks & Expands the comparison beyond the current four strategies and EmoBench & New strategy implementations, fair budgets, new datasets \\
\bottomrule
\end{tabularx}
\caption{Future work with motivation and resource needs.}
\label{tab:future}
\end{table}

% =============================================================================
% 6. Conclusion
% =============================================================================
\section{Conclusion}

We delivered a working, dockerised local tool and a controlled way to study
counterfactual editing strategies on emotional-reasoning questions. The main result is
not one high flip rate. It is a reproducible workflow that shows when a frozen model's
answer can be changed, how small and readable the change is, what kind of change it
was, and whether people judge it a useful explanation. On our measurements, discovery
search finds clearly more mechanical flips than the strict Sprint 2 baseline on
development data (25\% versus 10\% for Strategy 1) at a higher edit-size and call
cost, a harder unenriched set yields very few flips for Strategy 3 (1/20), and the one
fully reviewed run showed that verified, fluent, small edits can still all be invalid
as explanations. The honest separation of valid, diagnostic, not-found, and failed
outcomes, kept visible from the code through to the interface, is what makes those
statements trustworthy, and it is the part of this project we would keep first.

% =============================================================================
% References
% =============================================================================
\clearpage
\begin{thebibliography}{9}

\bibitem{bhattacharjee2024}
A. Bhattacharjee, R. Moraffah, J. Garland, and H. Liu,
``Zero-shot LLM-guided counterfactual generation: A case study on NLP model
evaluation,'' \emph{arXiv preprint arXiv:2405.04793}, 2024.

\bibitem{gat2024}
Y. Gat, N. Calderon, A. Feder, A. Chapanin, A. Sharma, and R. Reichart,
``Faithful explanations of black-box NLP models using LLM-generated
counterfactuals,'' in \emph{Proceedings of ICLR}, 2024.

\bibitem{miller2019}
T. Miller,
``Explanation in artificial intelligence: Insights from the social sciences,''
\emph{Artificial Intelligence}, vol. 267, pp. 1--38, 2019.

\bibitem{ross2021}
A. Ross, A. Marasovi\'c, and M. E. Peters,
``Explaining NLP models via minimal contrastive editing,'' in
\emph{Findings of ACL-IJCNLP}, pp. 3840--3852, 2021.

\bibitem{sabour2024}
S. Sabour et al.,
``EmoBench: Evaluating the emotional intelligence of large language models,'' in
\emph{Proceedings of the 62nd Annual Meeting of the Association for Computational
Linguistics}, 2024.

\bibitem{tenney2020}
I. Tenney et al.,
``The Language Interpretability Tool: Extensible, interactive visualizations and
analysis for NLP models,'' in \emph{Proceedings of EMNLP: System Demonstrations},
pp. 107--118, 2020.

\bibitem{wachter2018}
S. Wachter, B. Mittelstadt, and C. Russell,
``Counterfactual explanations without opening the black box: Automated decisions and
the GDPR,'' \emph{Harvard Journal of Law \& Technology}, vol. 31, no. 2,
pp. 841--887, 2018.

\end{thebibliography}

% =============================================================================
% Appendices
% =============================================================================
\clearpage
\appendix

\section{Report Names, Code Identifiers, and Prompt Versions}
\label{app:mapping}

The code, stored experiment metadata, and evaluation documents use the original
internal identifiers. Table~\ref{tab:mapping} maps them to the names used in this
report so every artifact remains traceable.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}L{4.3cm}L{5.2cm}Y@{}}
\toprule
\textbf{Report name} & \textbf{Code identifier} & \textbf{Prompt versions} \\
\midrule
Baseline (word-level greedy) & \texttt{s1\_word\_greedy} & none (no LLM proposer) \\
Strategy 1 strict & \texttt{s2\_llm\_propose\_verify} & \texttt{s2-proposer-v5-\allowbreak coherence-\allowbreak checklist} \\
Strategy 1 discovery & \texttt{s2\_llm\_propose\_verify\_\allowbreak discovery} & \texttt{s2-proposer-v8-2-\allowbreak discovery-\allowbreak focused-event} \\
Strategy 2 strict & \texttt{s4\_importance\_infilling} & \texttt{s4-infill-v1} \\
Strategy 2 discovery & \texttt{s4\_importance\_\allowbreak infilling\_\allowbreak discovery} & \texttt{s4-infill-v1} \\
Strategy 3 strict & \texttt{s6\_concept\_causal\_editing} & \texttt{s6-concept-v2-\allowbreak grounded-\allowbreak diverse}, repair \texttt{s6-concept-\allowbreak repair-v1} \\
Strategy 3 discovery & \texttt{s6\_concept\_causal\_\allowbreak editing\_\allowbreak discovery} & \texttt{s6-concept-v3-\allowbreak discovery-first}, repair \texttt{s6-concept-\allowbreak repair-v1} \\
Shared minimality shrink & post-processor & \texttt{postprocess-\allowbreak inserted-span-\allowbreak shrink-v2} \\
Shared fluency judge / polish & fluency service & \texttt{fluency-judge-\allowbreak v2-rubric-100}, \texttt{fluency-polish-\allowbreak v3-direct-span} \\
Frozen target & harness & \texttt{target-v2-chat-\allowbreak dynamic-choices} \\
\bottomrule
\end{tabularx}
\caption{Mapping between report names, code identifiers, and pinned prompt versions.}
\label{tab:mapping}
\end{table}

\section{Prompt Texts}
\label{app:prompts}

Every prompt sent to an LLM is versioned, and the version string is stored with each
experiment run. The instruction texts below are quoted from the code at the release
commit. Each proposer prompt is accompanied by a JSON user message containing the
scenario, choices, original answer, foil, requested count, and previously failed
attempts; the target and fluency prompts receive only what is shown.

\subsection{Frozen target prompt (\texttt{target-v2-chat-dynamic-choices})}

The system message is built from the actual choice letters (two to six choices are
supported):

\begin{lstlisting}[style=prompt]
You answer multiple-choice emotional reasoning questions. Return only one
letter A, B, C, or D. Do not explain.
\end{lstlisting}

The user message contains only the scenario and the choices. It never contains the
foil, the ground truth, or any strategy information:

\begin{lstlisting}[style=prompt]
Scenario
<scenario text>

Choices
A. <choice text>
B. <choice text>
...

Answer with exactly one letter.
Answer
\end{lstlisting}

\subsection{Strategy 1 strict proposer (\texttt{s2-proposer-v5-\allowbreak coherence-\allowbreak checklist})}

\begin{lstlisting}[style=prompt]
You propose minimal counterfactual rewrites for emotion-reasoning
multiple-choice scenarios. You may see the target foil, but the frozen
target model will not. Change an underlying event, outcome, relationship,
or observable behaviour rather than directly naming or paraphrasing the
target emotion or answer. Modify exactly one existing sentence and do not
append, prepend, split, or merge sentences. Prefer changing no more than
3 words and never change more than 6 words. Preserve the surrounding
facts, actors, tense, grammar, and narrative coherence. Rewrite only the
scenario, keep it fluent and realistic, do not mention option letters,
and do not copy or morphologically derive any option answer text in the
scenario. Do not add a sentence or clause whose purpose is to state how a
person feels; express the change through an observable event, outcome,
relationship, or behaviour. Do not propose an edit that makes unchanged
sentences contradict the modified fact. Keep the changed sentence about
the same focal person or event and preserve at least one meaningful
content word from that sentence. Never replace a focal person's outcome
with a reaction by the crowd or another bystander. Before returning each
rewrite, silently check every unchanged sentence for references to a fact
the rewrite removed. [quality examples omitted for space; they show one
good three-word outcome edit and three bad patterns to avoid]
Return exactly the requested number of distinct rewrites and use a
different change mechanism where possible. Treat avoid entries as prior
failures and do not repeat them. Return JSON only; modified_scenario is
required and changed_span, change_type, and rationale are optional:
{"rewrites":[{"modified_scenario":"...","changed_span":"...",
"change_type":"event|outcome|relationship|behaviour","rationale":"..."}]}.
\end{lstlisting}

\subsection{Strategy 1 discovery proposer
(\texttt{s2-proposer-v8-2-\allowbreak discovery-\allowbreak focused-event})}

\begin{lstlisting}[style=prompt]
You search for counterfactual scenario edits for an emotion-reasoning
multiple-choice task. You may see the target foil, but the frozen target
model will not. Rewrite only the scenario and keep every answer choice
unchanged. Make one focused, coherent change to an underlying event,
outcome, relationship, or observable behaviour that could support the
target answer. Keep the change concise, but do not force it into a fixed
word count when a larger local change is needed for coherence. Return the
complete scenario for every rewrite, not only the changed sentence. Keep
exactly the same number and order of sentences, copy every unchanged
sentence word-for-word, and replace only one existing sentence in place.
Do not append, prepend, split, or merge sentences. Preserve the focal
person, tense, unrelated facts, grammar, and references from unchanged
sentences. Do not state an emotion as a shortcut, mention an option
letter, or copy answer-option wording into the scenario. Treat avoid
entries as prior failures and propose distinct mechanisms where possible.
The rewrites array must contain exactly <count> distinct objects; do not
stop after fewer than <count>. The top-level object must contain only the
rewrites key. Never echo input keys such as scenario, choices,
constraints, count, foil, or avoid. Replace every placeholder in this
exact output shape and return JSON only: {"rewrites":[
{"modified_scenario":"<complete scenario rewrite 1>","changed_span":
"<changed text>","change_type":"event|outcome|relationship|behaviour",
"rationale":"<brief reason>"}, ...]}
\end{lstlisting}

\subsection{Strategy 2 infill proposer (\texttt{s4-infill-v1})}

\begin{lstlisting}[style=prompt]
You fill masked spans in emotion-reasoning multiple-choice scenarios.
You may see the target foil, but the frozen target model will not.
Only replace each [MASK] span. Preserve every unmasked character exactly,
keep the final scenario fluent and realistic, do not mention option
letters, and do not copy any option answer text into the scenario.
Return JSON only:
{"rewrites":[{"modified_scenario":"...","rationale":"..."}]}.
\end{lstlisting}

\subsection{Strategy 3 strict proposer (\texttt{s6-concept-v2-grounded-diverse})}

\begin{lstlisting}[style=prompt]
You propose causal-inspired concept interventions for emotion-reasoning
scenarios. Each edit must change exactly one allowed concept through one
contiguous span replacement while keeping every other fact and concept
fixed. Copy original_span exactly from the scenario. Return only the
replacement, not a rewritten full scenario. Do not mention option letters
or copy any option answer text. Return exactly the requested number of
edits. Within this response, use different concept classes and original
spans where possible. Do not repeat any concept/span/target tuple in
used_edits or avoid. Return JSON only:
{"edits":[{"concept_class":"time","original_span":"...",
"replacement_span":"...","source_value":"...","target_value":"...",
"rationale":"..."}]}.
\end{lstlisting}

\subsection{Strategy 3 discovery proposer (\texttt{s6-concept-v3-discovery-first})}

\begin{lstlisting}[style=prompt]
You propose causal-inspired concept interventions for emotion-reasoning
scenarios. Treat one coherent situational concept intervention, not a
fixed token count, as the unit of change. A multi-word replacement is
allowed when needed to express that one concept clearly. Each edit must
change exactly one allowed concept through one contiguous span
replacement while preserving the focal actor, focal event, and every
unrelated fact. Copy original_span exactly from the scenario. Return only
the replacement, not a rewritten full scenario. Do not mention option
letters or copy any option answer text. Return exactly the requested
number of edits. Prefer unused concept classes and original spans, and do
not repeat any concept/span/target tuple in used_edits or avoid. Return
JSON only: {"edits":[{"concept_class":"time","original_span":"...",
"replacement_span":"...","source_value":"...","target_value":"...",
"rationale":"..."}]}.
\end{lstlisting}

\subsection{Strategy 3 grounding repair (\texttt{s6-concept-repair-v1})}

\begin{lstlisting}[style=prompt]
Repair only the grounding of one causal-inspired concept edit. Return
exactly one edit with an original_span copied verbatim from the scenario
that occurs exactly once. Preserve the supplied concept_class,
replacement_span, source_value, and target_value; do not invent a
different intervention or rewrite the full scenario. Do not mention
option letters or copy answer text. Return JSON only in the same edits
schema.
\end{lstlisting}

\subsection{Minimality span shrink (\texttt{postprocess-\allowbreak inserted-span-\allowbreak shrink-v2})}

\begin{lstlisting}[style=prompt]
Reduce one inserted span by deleting unnecessary words only. Keep the
remaining words in their original order; do not paraphrase or add any new
word or fact. Do not rewrite the rest of the scenario, mention option
letters, name the target answer, or add emotion labels. Return JSON only
with exactly one rewrite whose original_span is the supplied
inserted_span verbatim and whose replacement_span is shorter and
non-empty: {"rewrites":[{"original_span":"...","replacement_span":"..."}]}.
\end{lstlisting}

\subsection{Fluency judge (\texttt{fluency-judge-v2-rubric-100})}

The judge receives only the edited scenario text as the user message. It never sees
the choices or the foil.

\begin{lstlisting}[style=prompt]
Evaluate only the English fluency and readability of the supplied
scenario. Do not judge whether the facts are correct, whether the
scenario is emotionally appropriate, or which multiple-choice answer it
supports. Use this fixed scale: 90-100 means natural and grammatical with
no meaningful reading problem; 75-89 means understandable with only a
minor issue; 40-74 means noticeably awkward; 0-39 means seriously broken
or hard to understand. Do not invent a grammar error for a normal
sentence and do not penalise harmless style choices. Return JSON only,
using an integer score, for example: {"score":95,"issues":[]}. Return at
most five short issues.
\end{lstlisting}

\subsection{Fluency polish (\texttt{fluency-polish-v3-direct-span})}

The polish sees only the changed span and the surrounding context, never the choices
or the foil.

\begin{lstlisting}[style=prompt]
Delete only unnecessary words from original_span while keeping all
remaining words in their original order. Use context only to judge
readability. Do not add, replace, reorder, or inflect words, introduce
facts, change sentence boundaries, or rewrite the full context. Return
JSON with exactly two keys: original_span copied verbatim from the input
and one non-empty replacement_span. replacement_span must contain only
words and punctuation already present in original_span.
\end{lstlisting}

\end{document}
