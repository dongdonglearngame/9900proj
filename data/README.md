# Data

Use `data/raw` for source datasets and `data/processed` for generated local artifacts.

Raw and processed data files are ignored by Git. Keep only lightweight notes and `.gitkeep` files in the repository.
