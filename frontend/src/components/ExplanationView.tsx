// TODO(P18-UI-2): original vs modified scenario side by side + coloured diff.
// Intended props (see src/types/api.ts): result: CounterfactualResult | null.
export function ExplanationView() {
  return <section className="panel placeholder">TODO: Explanation (ExplanationView)</section>;
}
