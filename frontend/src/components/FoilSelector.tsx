// TODO(P18-UI-1): pick a foil (cannot be the original answer).
// Intended props (see src/types/api.ts): choices: ChoiceMap | null, originalAnswer: ChoiceLetter | null,
// foil: ChoiceLetter | null, onFoilChange.
export function FoilSelector() {
  return <section className="panel placeholder">TODO: Foil (FoilSelector)</section>;
}
