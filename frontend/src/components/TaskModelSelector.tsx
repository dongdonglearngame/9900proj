// TODO(P18-UI-1): task / dimension / model / strategy selectors.
// Intended props (see src/types/api.ts): models: ModelInfo[], strategies: StrategyInfo[],
// selectedModel, selectedStrategy, onModelChange, onStrategyChange.
export function TaskModelSelector() {
  return <section className="panel placeholder">TODO: Run Setup (TaskModelSelector)</section>;
}
