// TODO(P18-UI-1): show the model's predicted answer + choice text.
// Intended props (see src/types/api.ts): prediction: PredictResponse | null, choices: ChoiceMap | null.
export function PredictionView() {
  return <section className="panel placeholder">TODO: Prediction (PredictionView)</section>;
}
