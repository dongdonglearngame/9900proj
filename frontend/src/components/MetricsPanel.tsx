// TODO(P18-UI-2): flip success, edit distance, changed fraction, calls, runtime.
// Intended props (see src/types/api.ts): metrics: CounterfactualMetrics | null.
export function MetricsPanel() {
  return <section className="panel placeholder">TODO: Metrics (MetricsPanel)</section>;
}
